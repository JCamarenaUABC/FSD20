@model IEnumerable<DoctorProject.TB_PARAMETERS>

@{
    ViewBag.Title = "Index";
}


    <p>
        <h4>Catalog Parameters</h4>
        <hr />
        @Html.ActionLink("Create New", "Create")
    </p>
<table id="example1" class="table table-bordered table-striped">
    <tr>
        <th></th>

        <th>
            @Html.DisplayNameFor(model => model.DESCR_PARAM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VAL1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VAL2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VAL3)
        </th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        <a href="@(Url.Action("Edit", new { id = item.ID_PARAM }))">
            <i class="fas fa-edit"></i>
        </a>
        <a href="@(Url.Action("Delete", new { id = item.ID_PARAM }))">
            <i class="fas fa-trash-alt"></i>
        </a>
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DESCR_PARAM)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.VAL1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.VAL2)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.VAL3)
    </td>
</tr>
}

</table>
